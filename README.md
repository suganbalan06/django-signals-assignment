# Django Signals Assignment

This project demonstrates:

1. Django Signals behaviour:
   - Synchronous execution
   - Same thread execution
   - Same database transaction

2. Custom Python Rectangle class:
   - Initialized with length and width
   - Iterable returning:
     {'length': value}
     {'width': value}

Tech used:
- Django
- Python
